import React, { FC, ReactNode } from 'react';
import * as Styled from './Sidebar.Styled';

export const Sidebar: FC = () => {
  return (
    <Styled.SidebarWrapper>Sidebar</Styled.SidebarWrapper>
  );
}