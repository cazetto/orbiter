import styled from '@emotion/styled';

interface RocketChatIframeTagProps {
  id: string;
  url: string;
  title: string;
}

export const RocketChatIframeTag = styled.div`
  background-color: green;
  width: 100%;
  border: none;
`;